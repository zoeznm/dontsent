# dontsent

RE:BOUND reply assistant for early dating messages and workplace replies.

## Runtime

The app expects Gemini credentials through environment variables:

```bash
GEMINI_API_KEY=...
GEMINI_MODEL=gemini-2.5-flash-lite
```

The API key is intentionally not committed.
